[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.30.2"
# release.yml is hand-edited to trigger only on `stow-cli-v*` tags, so
# dist must not flag it as out of date.
allow-dirty = ["ci"]
# CI backends to support
ci = "github"
# Only stow-cli is distributed; the workspace's other binary crates
# (stow-build, stow-admin, stow-mock-registry) are internal tooling.
packages = ["stow-cli"]
# The installers to generate for each app
installers = ["shell", "powershell"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Where to host releases
hosting = "github"
# Checksum of every release artifact
checksum = "sha256"
# Whether to install an updater program
install-updater = false
