[workspace]
resolver = "3"

members = [
    "build",
    "cli",
    "edge",
    "scheduler", "types",
    "watcher"
]

[workspace.dependencies]
zenwave = "0.3.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
stow-types = { path = "types" }
